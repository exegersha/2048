function CopyEnglish() as Object
    if (m.copyEnglish = invalid )
        m.copyEnglish = {
            ' Settings View
            WRITE_SETTINGS: "Write Settings"
            SET_DEFAULTS: "Set Defaults"
            VERSION: "Version"
        }
    end if

    return m.copyEnglish
end function
